<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bzz.cloud.rbac.dao.SysApiDao">

    <sql id="sysApiColumns">
						t.id AS id,
				t.name AS name,
				t.url AS url,
				t.sort AS sort,
				t.icon AS icon,
				t.is_show AS isShow,
				t.create_user_id AS createUserId,
				t.create_time AS createTime,
				t.update_user_id AS updateUserId,
				t.update_time AS updateTime,
				t.todo AS todo,
				t.remarks AS remarks,
				t.del_flag AS delFlag,
				t.version AS version
    </sql>

    <!--通过id查询-->
    <select id="get" resultType="com.bzz.cloud.rbac.entity.SysApi">
        select
        	<include refid="sysApiColumns"/>
        from sys_api t
        <where>
            t.del_flag = 0 AND t.id = #{id}
        </where>
    </select>

    <!--查询-->
    <select id="findList" resultType="com.bzz.cloud.rbac.entity.SysApi">
        select
        	<include refid="sysApiColumns"/>
        from sys_api t
        <where>
            t.del_flag = 0
        </where>
    </select>

    <!--插入-->
    <insert id="insert">
        INSERT INTO sys_api(
								id,
							name,
							url,
							sort,
							icon,
							is_show,
							create_user_id,
							create_time,
							update_user_id,
							update_time,
							todo,
							remarks,
							del_flag,
							version
        ) VALUES (
							 #{id},
							 #{name},
							 #{url},
							 #{sort},
							 #{icon},
							 #{isShow},
							 #{createUserId},
							 #{createTime},
							 #{updateUserId},
							 #{updateTime},
							 #{todo},
							 #{remarks},
							 #{delFlag},
							 #{version}

        )
    </insert>

	<!--保存api与authority的关系 -->
	<insert id="insertApiAndAuthority">
		INSERT INTO sys_api(
			id,
		    api_id,
			name,
			url,
			sort,
			icon,
			is_show,
			create_user_id,
			create_time,
			update_user_id,
			update_time,
			todo,
			remarks,
			del_flag,
			version
		) VALUES (
					 #{id},
					 #{name},
					 #{url},
					 #{sort},
					 #{icon},
					 #{isShow},
					 #{createUserId},
					 #{createTime},
					 #{updateUserId},
					 #{updateTime},
					 #{todo},
					 #{remarks},
					 #{delFlag},
					 #{version}

				 )
	</insert>

    <!-- 更新用户 -->
    <update id="update">
        UPDATE
			sys_api
        SET
						id =	#{id},
					name =	#{name},
					url =	#{url},
					sort =	#{sort},
					icon =	#{icon},
					is_show =	#{isShow},
					create_user_id =	#{createUserId},
					create_time =	#{createTime},
					update_user_id =	#{updateUserId},
					update_time =	#{updateTime},
					todo =	#{todo},
					remarks =	#{remarks},
					del_flag =	#{delFlag},
					version =	#{version}

    </update>

    <!-- 逻辑删除用户 -->
    <update id="deleteFalse">
        UPDATE
			sys_api
        SET
            del_flag = 1
        WHERE
            id = #{id}
	</update>


    <!-- 物理删除用户 -->
    <delete id="delete">
        DELETE FROM
            sys_api
        WHERE
            id = #{id}
	</delete>


</mapper>